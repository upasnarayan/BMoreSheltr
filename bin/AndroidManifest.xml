<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hophacks.bmoresheltr"
    android:versionCode="1"
    android:versionName="1.0" >
    
    <!-- permission to enable GPS -->
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/sheltr_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        
        <!-- make sure app is only used in portrait mode (android:screenOrientation) -->
        <activity
            android:name="com.hophacks.bmoresheltr.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
